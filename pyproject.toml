[project]
name = "ai-phone-assistant"
version = "0.1.0"
description = "Incrementally developed personal AI telephone agent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.21,<1",
    "fastapi>=0.116,<1",
    "jinja2>=3.1,<4",
    "httpx>=0.28,<1",
    "openai>=2.0,<3",
    "pydantic-settings>=2.10,<3",
    "python-multipart>=0.0.20,<1",
    "sqlalchemy>=2.0.43,<3",
    "twilio>=9.7,<10",
    "uvicorn[standard]>=0.35,<1",
    "websockets>=15,<16",
]

[dependency-groups]
dev = [
    "pytest>=8.4,<9",
    "ruff>=0.12,<1",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
